# Local/dev laptop: run `claude login` once, leave this blank.
# ANTHROPIC_API_KEY=
 
# Any environment: a console key - pay-as-you-go, takes priority if set.
ANTHROPIC_API_KEY=
 
# Which environment this agent is running in (gates ambient OAuth).
ENVIRONMENT=local
